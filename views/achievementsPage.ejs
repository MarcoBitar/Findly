<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Findly Achievements</title>
    <link rel="stylesheet" href="/css/achievementsPage.css">
    <link href="https://fonts.cdnfonts.com/css/just-me-again-down-here-2" rel="stylesheet">
</head>

<body>
    <%- include('partials/audio') %>
    <%- include('partials/header') %>
        <%- include('partials/menu') %>
            <h2>Achievements:</h2>
            <div class="flashcards-container">
                <% for(let i=0; i < achievements.length; i++) { %>
                    <a href="/Findly/achievements/<%= achievements[i].id %>"class="flashcard">
                        <h3>Name: <%= achievements[i].name %></h3>
                        <h4>Category: <%= achievements[i].category %></h4>
                        <p>Description: <%= achievements[i].descr %></p>
                        <p>Points Required: <%= achievements[i].pointsr %></p>
                    </a>
                <% } %>
            </div>
</body>
</html>