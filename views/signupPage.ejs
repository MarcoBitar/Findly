<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Findly Sign Up</title>
    <link rel="stylesheet" href="/css/signupPage.css">
    <link href="https://fonts.cdnfonts.com/css/just-me-again-down-here-2" rel="stylesheet">
</head>

<body>
    <div class="logo">Findly</div>
    <div class="container">
        <div class="signup-form">
            <h2>Sign up</h2>
            <form id="signupForm" method="POST" action="/Findly/users">
                <label for="username">Username</label> 
                <input type="text" id="username" name="name" placeholder="Username">
                <label for="email">Email</label>
                <input type="email" id="email" name="email" placeholder="user@company.com">
                <label for="password">Password</label>
                <input type="password" id="password" name="pass" placeholder="Password">
                <button type="submit">Continue</button>
            </form>
            <% if (typeof error !== 'undefined') { %> 
                <div style="color:brown; font-size: 1.3rem; margin-left: 5%; margin-top: -0.3rem; position:absolute;">
                    <%= error %>
                </div>
            <% } %>
            <div class="or">Or</div>
            <div class="login-link">
                <a href="/Findly/login">Already have an account? Log in</a>
            </div>
        </div>
    </div>
    <div class="footer">
        <a href="/Findly/privacy">Privacy</a>
        <a href="/Findly/terms">Terms</a>
    </div>
    <audio id="background-music" autoplay loop>
        <source src="/css/audio/there-be-pirates-endless-seas(background audio).mp3" type="audio/mp3">
    </audio>
    <script>
        window.onload = function() {
            const audio = document.getElementById('background-music');
            audio.muted = false;
            audio.play();
            audio.volume = 0.35;
        };
    </script>
</body>
</html>